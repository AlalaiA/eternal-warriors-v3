/* Pantalla REPORTS — en construcción */
export async function render(container, jugador, capital) {
  container.innerHTML = '<div class="screen-loading"><span>REPORTS — próximamente</span></div>';
}
