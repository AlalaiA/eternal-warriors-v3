/* Pantalla INVOCATIONS — en construcción */
export async function render(container, jugador, capital) {
  container.innerHTML = '<div class="screen-loading"><span>INVOCATIONS — próximamente</span></div>';
}
