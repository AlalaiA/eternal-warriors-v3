/* Pantalla SETTINGS — en construcción */
export async function render(container, jugador, capital) {
  container.innerHTML = '<div class="screen-loading"><span>SETTINGS — próximamente</span></div>';
}
